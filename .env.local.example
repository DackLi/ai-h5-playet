# 本地开发环境配置（复制此文件为 .env.local 进行本地覆盖）
# .env.local 会覆盖 .env.development 和 .env.production 中的同名变量
# 注意：.env.local 不会被 git 提交

# API 请求基础地址（本地开发可指向本地或测试服务器）
NEXT_PUBLIC_API_BASE_URL=https://video.beesads.com/drama/jowo/v1
# 环境标识
NEXT_PUBLIC_ENV=local
# 网站域名
NEXT_PUBLIC_SITE_URL=http://localhost:3000